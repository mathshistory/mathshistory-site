[model]
name = Categories Index
label = {{ this._slug }}
hidden = yes

[children]
model = category
hidden = yes

[fields.heading]
type = heading
heading = This is the categories section.

[fields.description]
type = info
label =
description = To edit a category, select one from the "Subpages" list on the left. To add a new category, click the "Add Page" button under "Page Actions" menu to the left.
